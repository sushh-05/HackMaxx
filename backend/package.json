{
  "name": "@hackmaxx/backend",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun src/local.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@hackmaxx/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "bun-types": "^1.4.2",
    "typescript": "^5.6.0"
  }
}